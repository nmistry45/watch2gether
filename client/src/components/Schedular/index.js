import React from 'react'
/**
 * The scheduler component will be use to schedule the watchgroup events
 * @returns The name of the scheduler component.
 */
function SchedularComp() {
    return (
        <div>SchedularComp</div>
    )
}
/**
 * The component is being exported as SchedularComp 
 * so that this component can be imported into other modules.
 */
export default SchedularComp